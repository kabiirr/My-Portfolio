.DS_Store

# Source media, kept locally as a backup of what's uploaded to ImageKit.
# Images are committed (~13MB) so the originals are never lost again;
# video is excluded because GitHub Pages shouldn't carry 30MB of MP4.
assets/media/*.mp4
